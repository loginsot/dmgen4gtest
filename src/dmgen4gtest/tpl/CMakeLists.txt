cmake_minimum_required(VERSION 3.2)

PROJECT({{PROJECT_NAME_LO}})

LIST(APPEND CMAKE_MODULE_PATH ${CMAKE_CURRENT_SOURCE_DIR}/cmake)
INCLUDE(cmake/ModuleImport.cmake)
INCLUDE(cmake/ModuleCompileOptions.cmake)
ModuleSetCompileOptions()
ModuleSetWinCompilerFlags()

SET({{PROJECT_NAME_UP}}_VERSION_MAJOR "1")
SET({{PROJECT_NAME_UP}}_VERSION_MINOR "0")
SET({{PROJECT_NAME_UP}}_VERSION_PATCH "1")
SET({{PROJECT_NAME_UP}}_VERSION "${{{PROJECT_NAME_UP}}_VERSION_MAJOR}.${{{PROJECT_NAME_UP}}_VERSION_MINOR}.${{{PROJECT_NAME_UP}}_VERSION_PATCH}")

MESSAGE(STATUS "VERSION: ${{{PROJECT_NAME_UP}}_VERSION}")

LibImport("gtest" "src/gtest")
ExeImport("test" "gtest")
